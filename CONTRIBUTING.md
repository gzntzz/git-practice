# Contributing

본 레포는 “기능 구현”보다 **협업 흐름 + 문서 품질**을 우선으로 굴림.

## Workflow
- Issue로 작업 정의 → 브랜치 생성 → PR → 리뷰 → 머지

## Branch Naming
- `feat/<topic>`: 기능
- `fix/<topic>`: 버그
- `docs/<topic>`: 문서
- `chore/<topic>`: 설정/잡무

## Commit Convention
- Conventional Commits 권장 (`feat:`, `fix:`, `docs:`, `chore:`)

## PR Rules
- PR은 작게(한 PR = 한 책임)
- 설명에 “무엇/왜/리스크/롤백” 최소 4줄은 남기기
